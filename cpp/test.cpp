/* #include "Model.hpp"

#include <iostream>
#include <vector>

using namespace std;

int main()
{
    ducks::Model model = ducks::Model(5,5);
    vector<vector <double>> A = model.getA();

    for (int i = 0; i < 5; i++)
    {
        for (int j = 0; j < 5; j++)
        {
            cout << A[i][j] << " ";
        }
        cout<<endl;
    }
} */
